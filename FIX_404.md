# 🔧 FIX LỖI 404 NOT_FOUND

## 🚨 Nguyên nhân

Lỗi 404 xảy ra khi:
1. ✅ Truy cập trang chủ: OK
2. ❌ Refresh page hoặc truy cập trực tiếp route: 404

**Lý do:** Server không biết phải trả về `index.html` cho tất cả routes.

---

## ✅ Giải pháp - ĐANG FIX

### **Đã cập nhật:**
1. ✅ `vite.config.js` - Thêm base config
2. ✅ `vercel.json` - Config rewrites
3. ✅ `netlify.toml` - Config redirects
4. ✅ `public/_redirects` - Netlify fallback

---

## 🔄 CHẠY CÁC LỆNH NÀY NGAY:

### **Bước 1: Build lại**
```bash
cd D:\QUANLYTHUCHI\webapp
npm run build
```

### **Bước 2: Push lên GitHub**
```bash
git add .
git commit -m "Fix 404 routing error"
git push
```

### **Bước 3: Đợi auto-deploy**
- Vercel/Netlify sẽ tự động deploy
- Đợi 1-2 phút

### **Bước 4: Test**
1. Vào app online
2. Click vào "Báo cáo"
3. Refresh page (F5)
4. ✅ Không bị 404 nữa!

---

## 🚨 NẾU VẪN BỊ LỖI

### **Vercel:**
1. Vào Vercel dashboard
2. Settings → General
3. Build & Development Settings:
   - Framework Preset: **Vite**
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. Redeploy

### **Netlify:**
1. Vào Netlify dashboard
2. Site settings → Build & deploy
3. Build settings:
   - Build command: `npm run build`
   - Publish directory: `dist`
4. Redeploy

---

## 💡 Giải thích

**React Router:**
- Client-side routing
- URL thay đổi nhưng không reload

**Problem:**
- Server nhận `/report`
- Không có file `report.html`
- → 404

**Solution:**
- Redirect mọi route về `/index.html`
- React Router xử lý từ đó
- → ✅ OK

---

## ✅ Test sau khi fix

```
https://your-app.vercel.app → ✅ OK
https://your-app.vercel.app/report → ✅ OK
https://your-app.vercel.app/add-income → ✅ OK
Refresh bất kỳ page nào → ✅ OK
```

---

**Chạy 3 lệnh trên là fix xong! 🚀**
